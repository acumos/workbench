export default {
  setWikiUrl(state, url) {
    state.wikiUrl = url;
  },
  setMsUrl(state, url) {
    state.mSurl = url;
  },
  setUserName(state, userName) {
    state.userName = userName;
  },
  setError(state, error) {
    state.error = error;
  },
  setMessage(state, message) {
    state.message = message;
  },
  setStatus(state, status) {
    state.status = status;
  }
};
